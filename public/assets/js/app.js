flatpickr('#date', {
    dateFormat: 'Y-m-d', // Asegúrate de usar un formato ISO
});
flatpickr('#time', {
    enableTime: true,
    noCalendar: true,
    dateFormat: 'H:i', // Usa un formato de hora ISO
});